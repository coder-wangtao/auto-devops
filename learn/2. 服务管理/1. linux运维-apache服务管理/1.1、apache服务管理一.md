apache服务管理一

1、安装和管理

 确认是否已经安装http服务 rpm -qa  |grep http 

如未安装请yum install  "http*"

当然如果你想源代码安装，也可以使用如下类似方式进行编译

./configure --prefix=/usr/local/apache;make;make install

启动和关闭、重启等

1）启动、关闭、状态、重启

service  httpd  start/stop/restart/status

systemctl  restart/start/stop/disable/enable   httpd.service     

2）语法检查命令

apachectl   -t

3）启动你的第一个web服务

a、用yum安装完http

b、修改配置文件/etc/httpd/conf/httpd.conf中      

​	listen 80  改成listen  你的ip:80

c、确认80端口是否开启         

​	#netstat  -ntpl |grep  :80

d、httpd.conf中屏蔽一行IncludeOptional conf.d/*.conf   

e、重启apache服务

f、echo "你的ip"  >/var/www/html/index.html

g、在浏览器中访问http://你的ip

2、日志解析

1）定制日志文件的格式涉及到两个指令，即LogFormat指令和CustomLog指令

定义日志格式

LogFormat "%h %l %u %t \"%r\" %>s %b" common

%...h: 远程主机   

%...l: 远程登录名字（来自identd，如提供的话）

%...u: 远程用户（来自auth；如果返回状态（%s）是401则可能是伪造的）

%...t: 以公共日志时间格式表示的时间（或称为标准英文格式）

%...r: 请求的第一行

%...s: 状态。对于进行内部重定向的请求，这是指*原来*请求的状态。如果用   %...>s，则是指后来的请求。

%...b: CLF格式的已发送字节数量，不包含HTTP头。

2）使用common格式的日志,并将日志输出到access_log这个文件中

CustomLog logs/access_log common

3）使用rotatelogs对日志进行轮转CustomLog "|/usr/local/apache2/bin/rotatelogs /usr/local/apache2/logs/%Y%m%d_access_img.linux.com 86400 480" common

4）错误日志的定义

#LogLevel: Control the number of messages logged to the error_log  

#Possible values include: debug, info,notice, warn, error, crit,

#alert, emerg.

LogLevel warn  //定义错误日志的级别ErrorLog logs/error_log  //定义错误日志的输出文件

备注：rotatelogs要写绝对路径,日志文件要写绝对路径

3、状态

常见的几种状态

200 OK 一切正常。

301  永久重定向

302 临时重定向

403 权限被拒绝

404 没找到

500 服务器内部错误

502 Bad Gateway

504 Gateway Timeout

4、重要参数

1）web服务进程所属的用户和组在主配置文件中，确认到的两行

User  apache

Group apache 

#ps -ef |grep httpd

2）对应域名放置资源的路径

DocumentRoot    "/var/www/html"

3）放置配置文件，默认日志文件存放的路径ServerRoot    "/etc/httpd"

4）定义域名ServerName www.example.com
