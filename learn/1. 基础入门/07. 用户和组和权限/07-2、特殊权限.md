特殊权限

1、suid特殊权限

​	SUID:用于二进制的文件当中，对目录是无效的！     

​	让一般用户在执行某些二进制的文件的时候，能够暂时拥有该程序(二进制文件)所有者的权限     使用该命令的所属用户的权限来运行，而不是命令执行者的权限    

​	例子： /usr/bin/passwd   /etc/shadow

​	测试实验           

​		1）在root用户的家目录下（在某一目录下执行ls命令时候，要看其目录对应的权限   ls  -l  /root）           

​		2）普通用户test 使用ls命令，提示权限不够，当在root用户中给ls命令加个SUID权限时候，就能正常执行了

2、sgid权限

 	对于文件，使用该命令的组群权限来运行 

​	对于目录，在设置了 sgid 权限的目录中创建的文件会继承该目录的组群身份,创建一个子目录的话，不但会继承组群身份，还会继承sgid的特殊权限位。

​	例子：/usr/bin/wall

3、sbit

   	粘滞位（Sticky Bit ）：在带有粘滞位的目录中的文件只能被文件的所属用户和根用户删除，不管该目录的写入权限是如何设置的（“ t ” 代表粘滞位）

​	/tmp本身的权限 1777

​     在具有sbit的目录下，用户若在该目录下具有w和x权限，则当用户在该目录下建立文件或目录时，只有文件的拥有者和root才有权利删除

4、特殊权限的设置

​	SUID/SGID/SBIT权限设置和前面说的rwx差不多，也有两种方式，

​	一种是字符方式，一种是数字方式。

​	4 为 SUID ＝ u+s

​	2 为 SGID ＝ g+s

​	1 为 SBIT ＝ o+t

​	小s和大S的区别，小t和大T的区别（大写位表示其位为空）

5、隐藏属性

[root@homeworknote test]# ll  t1-rw-r--r-- 1 root root 7 10月 17 20:37 t1

[root@homeworknote test]# rm  -rvf  t1rm: 无法删除"t1": 不允许的操作

[root@homeworknote test]# echo "456"  >> t1 bash: t1: 权限不够

 lsattr  t1  //显示隐藏属性

[root@homeworknote test]# lsattr  t1----i--------e- t1

chattr [+=-]  [Asacdistu]  文件或目录名   i:不能增删改   a:只能累加
