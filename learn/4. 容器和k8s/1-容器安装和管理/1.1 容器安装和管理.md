一、容器安装和管理

1、容器介绍

1）、Docker是基于Go语言实现的开源容器项目，诞生于2013年年初，最初发 起者是dotCloud公司。

2）、现在主流的Linux操作系统都已经支持Docker。例如，红帽公司的RHEL 6.5/CentOS 6.5往上的操作系统、Ubuntu14.04往上的操作系统，都已经在软件 源中默认带有Docker软件包。       

3）、Docker的构想是要实现“Build，Ship and Run Any App，Anywhere”， 即通过对应用的封装(Packaging)、分发(Distribution)、部署 (Deployment)、运行(Runtime)生命周期进行管理，达到应用组件“一次封装，到处运行”的目的。这里的应用组件，既可以是一个 Web应用、一个编译环境，也可以是一套数据库平台服务，甚至是一个操作系统或集群。

<img src="/Users/hellen/Library/Application Support/typora-user-images/image-20240731190716086.png" alt="image-20240731190716086" style="zoom:50%;" />



​						Docker与传统虚拟机的比较

<img src="/Users/hellen/Library/Application Support/typora-user-images/image-20240731190749515.png" alt="image-20240731190749515" style="zoom:50%;" />

2、docker安装

老版本安装方式

#yum install  https://dl.fedoraproject.org/pub/epel/epel-release-latest-8.noarch.rpm

#yum install https://download.docker.com/linux/fedora/30/x86_64/stable/Packages/containerd.io-1.2.6-3.3.fc30.x86_64.rpm

#curl https://download.docker.com/linux/centos/docker-ce.repo -o /etc/yum.repos.d/docker-ce.repo

#yum install   docker-ce     //会给比较新的版本给你安装



最新安装方式

#yum install  docker

3、容器、镜像、仓库

1）、仓库(Repository)是集中存放镜像的地方， 仓库可以被认为是一个具体的项目或目录。

2）、注册服务器(Registry)是管理仓库的具体服务器，每个服务器上可以有多个仓库，而每个仓库下面有多个镜像。

3）、例如对于仓库地址dl.dockerpool.com/ubuntu来说，dl.dockerpool.com是注册服务器(Registry)，ubuntu是仓库(Repository)

​	Docker Hub（公共仓库）登录——docker login注册成功后，本地用户目录的 .dockercfg中将保存用户的认证信(Linux中) 然后之后就能够进行之前学习的一些操作，如拉取镜像等  

​	私有仓库——docker-registrydocker-registry是官方提供的工具，可以用于构建私有的镜像仓库 有时候，我们并不想把自己的项目公开，只想自己使用。这时我们就需要自己建立自己的docker hub

4、基本操作

容器服务启动#systemctl  start   docker.service     //新版本不需要服务管理

注意：二进制安装时启动docker服务使用    dockerd  &

#docker  search    镜像名字   //搜索镜像 

#docker  pull  ubuntu     //从docker官方仓库下载镜像  

#docker images    //查看当前系统有哪些镜像



开启容器  

#docker run   -t  -i  ubuntu   /bin/bash     //docker  run  -d     -it   ubuntu    /bin/bash 

新建并启动一个容器，可以基于某个镜像开无数个容器  

这里的-t是让docker分配一个伪终端，-i是让容器的标准输入保持打开  也可以加-d 放后台运行

确认系统上有哪些容器 

#docker ps  -a     如果进入容器后，退出想重新进入原来的容器，可以使用如下步骤和命令来完成  

docker start 容器id  

＃docker attach  容器的名字   //也可以使用docker exec  或者第三方软件nsenter

docker  exec    -it      容器名字或容器id     /bin/bash  



镜像和容器的导出、导入、删除  

#docker save  -o   ubuntu_14.04.tar   ubuntu:14.04   //导出镜像  

#docker load  <   ubuntu_14.04.tar    //导入镜像    

#docker rmi   ubuntu   //删除镜像    

#docker  rm   容器id   //删除容器  

#docker export  容器id的前4个字符   > test_con.tar  //导出容器  

#cat  test_con.tar |docker import  -  test/ubunut:v1.0 //导入容器 



容器的存储－数据卷  

#docker run -d  -P   --name  web  -v  /webapp   train/webapp  python  app.py  

-d 后台运行一个容器  

-P 允许外部访问容器需要开放的端口  

--name web  运行容器时起的名字web  -v /webapp  创建一个数据卷并挂载在容器的/webapp目录下  

train/webapp  该容器创建的时候所基于的镜像名字  

python app.py 创建好容器时运行的命令



#docker run -d  -P   --name  web  -v  /lcoal/dir:/docker/dir:ro   train/webapp  python  app.py  

-v /local/dir:/docker/dir:ro  /local/dir是宿主机的目录，/docker/dir是容器内的目录，ro是说挂载到容器来的目录只读，未加ro的话默认是rw    

#docker run -d -P --name web -v ~/.bash_history:/.bash_history ubuntu /bin/bash  

也可以从主机挂载单个文件到容器中做为数据卷  数据卷容器  

#docker run -it  --name  ubuntu_c -v    /dbdata      docker.io/ubuntu   	

/dbdata目录不存在的话，也会自动在容器中创建出来   	

利用docker.io/ubuntu的镜像，建立一个叫ubuntu_c的容器，数据卷为/dbdata  

#docker run  -it --name db1 --volumes-from    ubuntu_c      docker.io/ubuntu	

利用docker.io/ubuntu的镜像，建立容器db1，挂载ubuntu_c容器中的数据卷

创建容器并进入容器   

#docker run -d  -P  -it  --name  ubuntu_test_volume  -v  /local/dir:/docker/dir    ubuntu  /bin/bash

#docker       exec  -it  ubuntu_test_volume      /bin/bash



1）一个是允许映射容器内应用的服务端口到本地宿主主机。	当使用-P（大写的）标记进，Docker 会随机映射一个端口到内部容器开放的网络端口	

docker run -it -P httpd	

使用-p来指定端口映射       

docker run -d -p 5000:80 httpd:latest	

docker run -d -p 5001:5001 -p 3000:80 httpd	

docker run -d -p 127.0.0.1:5000:80 httpd	

docker  run  -it   -p  5000:80 docker.io/imagine10255/centos6-lnmp-php56   /bin/bash	

docker logs  -f   加镜像名字   //确认刚刚做的映射

2）一个是互联机制实现多个容器间通过容器名来快速访问	

容器连接（linking）是另一种方式，它会在源和接受的容器之间创建一个隧道，接收的容器可以看到源容器的信息。

备注：容器有自己的内部网络和IP地址，使用docker inspect + 容器ID可以获取容器的具体信息
