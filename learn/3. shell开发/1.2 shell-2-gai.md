shell-1

2、流程控制和函数

if...else...fi

#vim   ifelseif.sh  //脚本例子

#!/bin/bash
#function test if else
#author xxx  20131202
#set -x
echo "Press y to continue"
read yn
if [ "$yn" = "y" ]; then
	echo "the script continue"
else
	echo "the script stoped"
fi

#vim ifelsefi1.sh  //脚本例子

#/bin/bash
#function test if else
#author xxx  20131202
echo "Press y or yes to continue"
read yn
#if [ "$yn" = "y" ] && [ "$yn" = "yes" ]; then
if [ "$yn" = "y" ] || [ "$yn" = "yes" ]; then
#if [ "$yn" = "y" -o "$yn" = "yes" ]; then
        echo "the script continue"
else
        echo "the script stoped"
fi

a在[  ]和[  ]中，可以使用||  和&& 来进行连接，||表示或，&&表示而且b [  ]中间注意空格



test脚本例子：

#!/bin/bash
#functon
#author
#if [ -f ifelsefi.sh ] && [ ! -x ifelsefi.sh ] ;then
if [ -f ifelsefi.sh  -a ! -x ifelsefi.sh ] ;then
	echo " ifelsefi.sh not have execute"
else
	echo "ifelsefi.sh have execute "
fi



case脚本例子

#!/bin/bash
#function case
#author  xxx  20131202
echo "Please select one \ two \ three"
read number
case $number in
one)
	echo "You select one!"
	;;
two)
	echo "You select two"
	;;
three)
	echo "You select three"
	;;
*)
	echo  100
	#exit 100
esac



select 菜单方式脚本例子

#!/bin/bash
#function: 选择菜单demo
#author: 20200101 sss
#select choice

PS3="Please enter you select install menu:"
select i in http mysql php config  all exit
do

case $i in
    http)
    	echo "install  http ..........."
    ;;
    mysql)
    	echo "install  mysql ........."
    ;;
    php)
    	echo "install  php .........."
    ;;
    config)
    	echo "config .........."
    ;; 
    all)
    	echo "all ............"
    ;;
    exit)
    echo "The system exit"
    exit
esac

for循环脚本例子

例子1:

#!/bin/bash
#function for1
#author xxx 20131202
declare -i i
declare -i s=0
for ((i=1;i<=100;i=i+1))
do
	s=s+i
done
echo "1..100 the sum is $s"

例子2:

#!/bin/bash
#function for2
#author xxx 20131202
for file in `ls`
#for file in $(ls)
do
	echo "file=$file"
done

例子3:

#!/bin/bash
#function for3
#author xxx 20131202
#for i in $(seq  0 39)
echo  $0
#for i in $(seq -f '%02.1f' 0 39)
#for i in $(seq -f '%03.1f' 0 39)
for i in $(seq -w 0 999)
#for i in $(seq  0 39)
do
	echo "$i"
done



while..do..done脚本例子：

#!/bin/bash
#function while example
#author xxx 20131202
declare -i i=0
declare -i s=0
while [ $i != 101 ]
do	
	s=s+i
	i=i+1
done
echo "1..100 the sum is $s"

死循环脚本例子：

#!/bin/bash
#function while example
#author xxx 20131202
while [ 1 != 0 ]
do
	sleep 1
	date	
done

本地函数调用例子

#!/bin/bash
#function:loacal function
#author xxx  20131202
function logfunc
{
	echo " `date` local function is running ...." |tee  -a /var/log/messages	
}
#调用函数
logfunc

外部函数调用例子

#!/bin/bash
function logfunc
{
        echo " `date` local function is running ...." |tee  -a /var/log/messages
}#logfunc

调用前面脚本中的函数

#!/bin/bash
source  /data/linux/shell/logfunc
logfunc

引用函数中的变量

#!/bin/bash
#函数名字命名的时候跟变量命名的时候一样，比如fun-1，这个命名就不对了
function1()
	{
		local i=123
		b=2
		echo -e "function inner \$i=$i  \$b=$b \n"
	}

function1
echo -e "function outer \$i=$i \$b=$b \n"



测试和练习

1）要求编写一脚本，判断是否存在目录/usr/local/diy,如果不存在则建立并在该目录下建立一个新文件diy1，如果已经存在则该目录则继续判断是否有文件diy1,如没有也请新建一个。

2）在/tmp/tempdir目录下建立文件名为001...999的999文件，文件内容为对应的文件名，如001的文件内容为001。

3）在给定的数组（1  9 8  6 10 11  ）中 找出最大的那个数输出到屏幕。

4）要求每隔3秒获得隔壁同学的服务器负载情况
