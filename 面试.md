plugins包：
deploy-plugin: k8s部署
docker-plugin: docker build dockfile
nginx-plugin: nginx.config
build-plugin: npm i、npm run build
git-plugin: git拉代码
test-plugin: 单测

web页面、server 1.展示所有插件，执行插件2.应用类型(针对不同的项目类型：比如后端项目，后台项目 h5项目)，每一种应用类型对会对应一种工作流。3.应用(就是每一个具体项目)；4.工作流(就是执行一个个的插件) 代码回滚？

plugin-sdk: 调用plugin的工具函数

engine: 1.workflow存储、执行流程逻辑;2.plugin存储、执行逻辑;3.资源获取存储清理分配资源;4.任务调度器

k8s代码回滚
Deployment 利用历史 ReplicaSet，把流量和副本数恢复到旧版本。

Directed Acyclic Graph
有向无环图

<!--
用户输入
   ↓
意图识别
   ↓
知识库检索 ──→ 网页搜索
   ↓              ↓
    数据融合处理
           ↓
        大模型生成
           ↓
         输出结果
-->
